import { readFile } from "node:fs/promises";import { auditY4FOverlap,evaluateY4F,generateY4FManifest,writeJson,type Y4FManifest } from "../evaluation/y4fHoldout.service";
const [mode,manifestPath,outputPath,calibrationPath,datasetRoot,limitRaw]=process.argv.slice(2);(async()=>{if(mode==="generate"){const m=await generateY4FManifest({datasetRoot:datasetRoot!,calibrationManifest:calibrationPath!,perFamily:50});await writeJson(manifestPath!,m);await writeJson(outputPath!,auditY4FOverlap(m));return}const m=JSON.parse(await readFile(manifestPath!,"utf8")) as Y4FManifest;await writeJson(outputPath!,await evaluateY4F(m,outputPath!+"-work",limitRaw?Number(limitRaw):undefined));})().catch(e=>{console.error(e instanceof Error?e.stack:e);process.exitCode=1});
