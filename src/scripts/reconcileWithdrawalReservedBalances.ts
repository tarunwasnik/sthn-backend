import "dotenv/config"; import mongoose from "mongoose"; import { withdrawalReservedBalanceReconciliationService } from "../services/financial/withdrawalReservedBalanceReconciliation.service";
async function main() { if (!process.env.MONGODB_URI) throw new Error("MONGODB_URI is required"); const args = process.argv.slice(2); const dryRun = !args.includes("--apply"); const limitArg = args.find((arg) => arg.startsWith("--limit=")); const limit = Number(limitArg?.split("=")[1] ?? 100); await mongoose.connect(process.env.MONGODB_URI!); try { console.log(JSON.stringify(await withdrawalReservedBalanceReconciliationService.reconcile({ dryRun, limit }), null, 2)); } finally { await mongoose.disconnect(); } } main().catch((e) => { console.error(e instanceof Error ? e.message : e); process.exitCode = 1; });
