import fs from "fs"; import path from "path";
const root = path.resolve(__dirname, ".."); const read = (...p: string[]) => fs.readFileSync(path.join(root, ...p), "utf8"); const expect = (ok: boolean, message: string) => { if (!ok) throw new Error(message); };
const repo = read("repositories", "walletTopUpRequest.repository.ts"); const service = read("services", "financial", "adminWalletTopUpDecision.service.ts"); const route = read("routes", "v1", "admin.financial.routes.ts"); const controller = read("controllers", "adminWalletTopUpDecision.controller.ts");
expect(repo.includes("status: WalletTopUpRequestStatus.PENDING") && repo.includes("approvePending") && repo.includes("rejectPending"), "Guarded pending transitions missing."); expect(!repo.includes("updateStatus") && !repo.includes("genericUpdate"), "Generic status mutation found."); expect(service.includes("WalletTopUpDecision.APPROVE") && service.includes("WalletTopUpDecision.REJECT") && service.includes("WALLET_TOP_UP_REQUEST_DECISION_CONFLICT"), "Decision/replay contract incomplete."); expect(!service.includes("ledger") && !service.includes("provider") && !service.includes("Wallet.create"), "Financial side effect found."); expect(controller.includes("req.user.id") && !controller.includes("decidedBy"), "Admin identity boundary invalid."); expect(route.includes('router.patch("/wallet-top-up-requests/:topUpReference/decision", decideWalletTopUpRequest)'), "Admin decision route missing."); console.log("Phase 7D wallet top-up decision validation passed.");
