import { NextFunction, Request, Response } from "express";
import { InternalTopUpFundingOutcome } from "../enums/financial/internalTopUpFundingOutcome.enum";
import { InternalTopUpFundingFailureCode } from "../enums/financial/internalTopUpFundingFailureCode.enum";
import { topUpFundingOrchestratorService } from "../services/financial/topUpFundingOrchestrator.service";
export const startWalletTopUpProcessing = async (req: Request, res: Response, next: NextFunction) => { try { const body = req.body; if (!req.user) return res.status(401).json({ success: false, message: "Unauthorized" }); if (!body || typeof body !== "object" || Array.isArray(body) || !Object.values(InternalTopUpFundingOutcome).includes(body.outcome) || Object.keys(body).some((key) => !["outcome", "failureCode", "failureReason"].includes(key)) || (body.outcome === InternalTopUpFundingOutcome.FAILURE && !Object.values(InternalTopUpFundingFailureCode).includes(body.failureCode)) || (body.outcome === InternalTopUpFundingOutcome.SUCCESS && (body.failureCode || body.failureReason))) return res.status(400).json({ success: false, message: "Invalid top-up funding simulation." }); const data = await topUpFundingOrchestratorService.start({ topUpReference: req.params.topUpReference, outcome: body.outcome, failureCode: body.failureCode, failureReason: typeof body.failureReason === "string" ? body.failureReason.trim() || undefined : undefined }); return res.status(200).json({ success: true, data }); } catch (error) { next(error); } };
