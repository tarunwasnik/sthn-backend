import { NextFunction, Request, Response } from "express";
import { topUpAccountingOrchestratorService } from "../services/financial/topUpAccountingOrchestrator.service";
export const completeWalletTopUpAccounting = async (req: Request, res: Response, next: NextFunction) => { try { if (!req.user) return res.status(401).json({ success: false, message: "Unauthorized" }); if (req.body && Object.keys(req.body).length) return res.status(400).json({ success: false, message: "Accounting request body is not allowed." }); return res.status(200).json({ success: true, data: await topUpAccountingOrchestratorService.complete(req.params.topUpReference) }); } catch (error) { next(error); } };
