"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const strict_1 = __importDefault(require("node:assert/strict"));
const node_test_1 = require("node:test");
const mongoose_1 = require("mongoose");
const User_1 = __importDefault(require("../../models/User"));
const auditLog_model_1 = require("../../models/auditLog.model");
const booking_model_1 = require("../../models/booking.model");
const chat_model_1 = require("../../models/chat.model");
const dispute_model_1 = require("../../models/dispute.model");
const disputeInvestigationSubmission_model_1 = require("../../models/disputeInvestigationSubmission.model");
const adminDispute_controller_1 = require("../../controllers/adminDispute.controller");
const disputeInvestigation_controller_1 = require("../../controllers/disputeInvestigation.controller");
const database_1 = require("../financial/phase7h/helpers/database");
process.env.NODE_ENV = "test";
(0, node_test_1.before)(async () => (0, database_1.connectPhase7HDatabase)(), { timeout: 120000 });
(0, node_test_1.beforeEach)(async () => (0, database_1.clearPhase7HDatabase)());
(0, node_test_1.after)(async () => (0, database_1.disconnectPhase7HDatabase)(), { timeout: 30000 });
function response() { const r = {}; r.response = { status: (n) => { r.statusCode = n; return r.response; }, json: (b) => { r.body = b; return r.response; } }; return r; }
async function fixture() { const id = new mongoose_1.Types.ObjectId().toString(); const [customer, creator, admin] = await Promise.all([User_1.default.create({ email: `di2c-u-${id}@x.test`, password: "x", status: "active", governanceState: "ACTIVE" }), User_1.default.create({ email: `di2c-c-${id}@x.test`, password: "x", status: "active", governanceState: "ACTIVE" }), User_1.default.create({ email: `di2c-a-${id}@x.test`, password: "x", role: "admin", status: "active", governanceState: "ACTIVE" })]); const booking = await booking_model_1.Booking.create({ slotIds: [new mongoose_1.Types.ObjectId()], userId: customer._id, creatorId: creator._id, serviceId: new mongoose_1.Types.ObjectId(), serviceTitle: "Evidence", durationMinutes: 30, price: 100, currency: "INR", status: "CANCELLED", paymentStatus: "PAID", isPayable: true, isPayoutEligible: false, isFinancialLocked: false, expiresAt: new Date(Date.now() + 999999), hasInteracted: true, serviceAmount: 100, platformFeeAmount: 0, commissionAmount: 20, creatorAmount: 80, totalAmount: 100 }); const dispute = await dispute_model_1.Dispute.create({ bookingId: booking._id, raisedBy: customer._id, raisedByRole: "USER", reason: "Investigation", status: "OPEN" }); return { customer, creator, admin, booking, dispute }; }
async function submit(actor, disputeId, body) { const r = response(); await (0, disputeInvestigation_controller_1.createParticipantSubmission)({ user: { id: String(actor._id) }, params: { disputeId }, body }, r.response); return r; }
async function read(actor, disputeId) { const r = response(); await (0, disputeInvestigation_controller_1.getParticipantInvestigation)({ user: { id: String(actor._id) }, params: { disputeId }, query: {} }, r.response); return r.body; }
(0, node_test_1.test)("DI-2C creates append-only private participant submissions and preserves supported Chat evidence", async () => { const f = await fixture(); const attachment = await chat_model_1.Chat.create({ bookingId: f.booking._id, senderId: f.customer._id, senderRole: "USER", type: "image", message: "proof", attachment: { url: "https://res.cloudinary.com/test/image/upload/v1/proof.jpg", publicId: "proof", fileName: "proof", originalFileName: "proof.jpg", mimeType: "image/jpeg", fileSize: 9, resourceType: "image" }, seenBy: [f.customer._id], aiFlags: [], isDeleted: false, reactions: [] }); const made = await submit(f.customer, String(f.dispute._id), { kind: "EVIDENCE", text: "My evidence", evidence: [{ chatMessageId: String(attachment._id), caption: "receipt" }], submittedBy: "ADMIN", sharedWithCounterparty: true }); strict_1.default.equal(made.statusCode, 201); strict_1.default.equal(made.body.submission.branch, "CUSTOMER"); strict_1.default.equal(made.body.submission.evidence[0].type, "IMAGE"); strict_1.default.equal(await disputeInvestigationSubmission_model_1.DisputeInvestigationSubmission.countDocuments({ disputeId: f.dispute._id }), 1); await strict_1.default.rejects(() => submit(f.creator, String(f.dispute._id), { kind: "EVIDENCE", evidence: [{ chatMessageId: String(attachment._id) }] }), /Evidence must reference your existing/); });
(0, node_test_1.test)("DI-2C enforces branch controls and hides unshared counterparty content", async () => { const f = await fixture(); await submit(f.customer, String(f.dispute._id), { kind: "STATEMENT", text: "customer private" }); await submit(f.creator, String(f.dispute._id), { kind: "STATEMENT", text: "creator private" }); const customer = await read(f.customer, String(f.dispute._id)); const creator = await read(f.creator, String(f.dispute._id)); strict_1.default.equal(customer.submissions.length, 1); strict_1.default.equal(creator.submissions.length, 1); strict_1.default.equal(customer.submissions[0].branch, "CUSTOMER"); strict_1.default.equal(creator.submissions[0].branch, "CREATOR"); f.dispute.customerInput.state = "CLOSED"; await f.dispute.save(); await strict_1.default.rejects(() => submit(f.customer, String(f.dispute._id), { kind: "STATEMENT", text: "blocked" }), /input is closed/); });
(0, node_test_1.test)("DI-2C Admin requests are target-filtered and explicit sharing is item-specific and replay-safe", async () => { const f = await fixture(); const first = await submit(f.customer, String(f.dispute._id), { kind: "STATEMENT", text: "share me" }); await submit(f.customer, String(f.dispute._id), { kind: "STATEMENT", text: "do not share" }); const request = response(); await (0, adminDispute_controller_1.createAdminDisputeRequest)({ user: { id: String(f.admin._id), role: "admin" }, params: { disputeId: String(f.dispute._id) }, body: { target: "CUSTOMER", text: "Please clarify" } }, request.response); strict_1.default.equal(request.statusCode, 201); strict_1.default.equal((await read(f.customer, String(f.dispute._id))).adminRequests.length, 1); strict_1.default.equal((await read(f.creator, String(f.dispute._id))).adminRequests.length, 0); const share = response(); await (0, adminDispute_controller_1.shareAdminDisputeSubmission)({ user: { id: String(f.admin._id), role: "admin" }, params: { disputeId: String(f.dispute._id) }, body: { submissionReference: first.body.submission.submissionReference } }, share.response); strict_1.default.equal(share.body.changed, true); const creator = await read(f.creator, String(f.dispute._id)); strict_1.default.equal(creator.submissions.length, 1); strict_1.default.equal(creator.submissions[0].text, "share me"); const replay = response(); await (0, adminDispute_controller_1.shareAdminDisputeSubmission)({ user: { id: String(f.admin._id), role: "admin" }, params: { disputeId: String(f.dispute._id) }, body: { submissionReference: first.body.submission.submissionReference } }, replay.response); strict_1.default.equal(replay.body.changed, false); strict_1.default.equal(await auditLog_model_1.AuditLog.countDocuments({ action: "DISPUTE_SUBMISSION_SHARED", entityId: f.dispute._id }), 1); const adminRead = response(); await (0, adminDispute_controller_1.getAdminDisputeInvestigation)({ params: { disputeId: String(f.dispute._id) }, query: {} }, adminRead.response); strict_1.default.equal(adminRead.body.customerBranch.length, 2); });
(0, node_test_1.test)("DI-2C terminal disputes reject submissions, requests, and sharing without financial or governance changes", async () => { const f = await fixture(); const item = await submit(f.customer, String(f.dispute._id), { kind: "STATEMENT", text: "existing" }); f.dispute.status = "REJECTED"; await f.dispute.save(); await strict_1.default.rejects(() => submit(f.customer, String(f.dispute._id), { kind: "STATEMENT", text: "blocked" }), /OPEN dispute/); await strict_1.default.rejects(() => (0, adminDispute_controller_1.createAdminDisputeRequest)({ user: { id: String(f.admin._id), role: "admin" }, params: { disputeId: String(f.dispute._id) }, body: { target: "BOTH", text: "blocked" } }, response().response), /OPEN dispute/); await strict_1.default.rejects(() => (0, adminDispute_controller_1.shareAdminDisputeSubmission)({ user: { id: String(f.admin._id), role: "admin" }, params: { disputeId: String(f.dispute._id) }, body: { submissionReference: item.body.submission.submissionReference } }, response().response), /OPEN dispute/); strict_1.default.equal((await User_1.default.findById(f.customer._id).lean())?.governanceState, "ACTIVE"); strict_1.default.equal((await booking_model_1.Booking.findById(f.booking._id).lean())?.status, "CANCELLED"); });
